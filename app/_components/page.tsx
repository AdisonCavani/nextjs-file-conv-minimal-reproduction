type Props = {
  className?: string;
};

function PageComponent({ className }: Props) {
  return <div className={className}>In progress... Stay tuned!</div>;
}

export default PageComponent;
